# herdr runtime artifacts (regenerated at runtime; never commit)
home/.config/herdr/*.log
home/.config/herdr/session.json
home/.config/herdr/*.sock

# nix build noise
result
result-*

# Home Manager adoption backups (created by `switch -b backup`)
*.backup
*.backup-*

# WSL/Windows interop noise
*:Zone.Identifier
